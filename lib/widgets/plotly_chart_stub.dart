import 'package:flutter/material.dart';

// Mobilde derleme hatası almamak için boş bir fonksiyon (Stub)
Widget buildPlotlyWebView(Map<String, dynamic> config, double height, bool darkMode) {
  return const SizedBox();
}